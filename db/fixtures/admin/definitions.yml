def_embedded:
  name: "*embedded*"
  description: the right to start processes embedded in launch items
  domain: admin

def_untracked:
  name: "*untracked*"
  description: the right to launch processes from any URI
  domain: admin

def_simple:
  name: simple sequence
  uri: sequence1.rb
  description: a simple sequence

def_leave:
  name: leave request
  uri: leave_request1.rb
  description: "may I leave the office ?"
  launch_fields: "{\"employee\":\"\",\"from_date\":\"\",\"to_date\":\"\",\"reason\":\"\"}"

def_revision:
  name: revision
  uri: /definitions/revise.rb
  description: Revision d un objet PLM
  launch_fields: "{}"

def_promote:
  name: promotion
  uri: /definitions/promote.rb
  description: Promotion d un objet PLM
  launch_fields: "{}"
