# Sample localization file for English. Add more files in this directory for other locales.
# See http://github.com/svenfuchs/rails-i18n/tree/master/rails%2Flocale for starting points.

en:
  #########################################
  # modules
  #########################################
  # plm_object_controller_module
  ctrl_datafile: File

  #########################################
  # helpers
  #########################################
  # application
  h_show_hide_tree: Show/Hide tree
  h_sort_by_field: Sort by this field
  h_open_close_all_tree: Expand/Close all
  legend_tree_down: Explorer
  legend_tree_up: Referencer
  img_checkout: Checkout
  img_checkin: Checkin
  select_context: Context selection
  select_view: View selection
  # help
  help_root_title: SYLRPLM Documentation
  help_no_help_title: Help missing
  help_no_help: No help for %{key}
  help_lnk_acces: Go
  help_summary: Summary
  help_jump: View
  help_previous: Previous
  help_next: Next
  help_upper: Upper
  help_main: Main
  # processes
  label_more: More
  label_less: Less

  ###########################
  # plugins
  ###########################
  # will_paginate
  label_previous: Previous
  label_next: Next

  ###########################
  # formattage
  ###########################

  activerecord:
    errors:
      messages:
        # sylrplm
        status_not_valid: "Bad status"
        exception_validity: "Exception during %{model} validating:<br/>%{exception}"
        datafile_write_bad_format: The file %{filename} could not be saved, choose another volume.
       #sylrplm_common
        type_values_type_badly_defined: "%{key}=%{value} : type is badly defined: %{field}"
        type_values_value_undefined: "%{key}=%{value}: type is undefined"
        type_values_value_not_found: "%{key}=%{value}: value not found"
        type_values_string_too_long: "%{key}=%{value} is too long: %{value.length} > %{nbc}"
        type_values_value_not_type: "%{key}=%{value}: is not a %{type}"
        type_values_value_bad_format: "%{key}=%{value}: is a %{type} but is badly formatted %{fmt}"
        type_values_value_outside_limit: "%{key}=%{value} of type %{type} is outside the limit: %{sign} %{limit}"
        #typesobject
        field_badly_formatted: "Field of type '%{type}' is badly formatted, key='%{key}', fields=%{fields} exception=%{exception}"