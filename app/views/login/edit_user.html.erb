<% form_for :user do |form| %>
<%= error_messages_for 'user' %>
<%= render(:partial => "login/edit",:locals => { :form => form, :fonct=>"edit" }) %>
<table class='menu_bas'>
    <tr>
        <td>
            <%= submit_tag t("update"), :class => "submit" %>
        </td>
        <td>
            <%= link_to t("list"), "/login/list_users" %>
        </td>
    </tr>
</table>
<%end %>