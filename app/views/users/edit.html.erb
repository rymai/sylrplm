<h1><%=t(:user_edit) %></h1>
<% form_for @the_user do |form| %>
<%= form.error_messages %>
<%= render(:partial => "users/edit",:locals => { :form => form, :fonct=>"edit" }) %>
<table class='menu_bas'>
    <tr>
        <td>
            <%= submit_tag t("update") %>
        </td>
        <td>
            <%= link_to t("explorer"), @current_user %>
        </td>
        <td>
            <%= link_to t("list"), users_path %>
        </td>
    </tr>
</table>
<%end %>