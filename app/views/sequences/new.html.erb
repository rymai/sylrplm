<h1><%=t(:sequence_new) %></h1>
<% form_for(@sequence) do |f| %>
<%= f.error_messages %>
<%= render(:partial => "sequences/edit",:locals => { :f => f, :fonct=>"new" }) %>
<table class='menu_bas'>
    <tr>
        <td>
            <%= f.submit t("create") %>
        </td>
        <td>
            <%= link_to t("list"), sequences_path %>
        </td>
    </tr>
</table>
<% end %>