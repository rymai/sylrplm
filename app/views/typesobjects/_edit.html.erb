<fieldset>
	<legend>
		<%= t(:legend_typesobject) %>
	</legend>
	<%= f.label t(:label_object) %>
	<%= f.select(:forobject, @objectswithtype) %>
	<br/>
	<%= f.label t(:label_name) %>
	<%= f.text_field :name %>
	<br/>
	<%= f.label t(:label_description) %>
	<%= f.text_area :description %>
	<br/>
	<%= f.label t(:label_fields) %>
	<br/>
	<%= h_input_domain(f) %>
	<br />
	<!--:<%= @typesobject.fields%>:<%= @typesobject.fields == "\"\""%>-->
	<%= render(
	:partial => "shared/form_json",
	:locals => {
	:fields => @typesobject.fields ,
	:form_id => form_id,
	:textarea_name => 'typesobject[fields]',
	:options => {"buttons" => "all"} }
	) %>
	
</fieldset>