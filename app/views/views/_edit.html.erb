<fieldset>
	<legend>
		<%= t(:legend_view) %>
	</legend>
	<p>
		<%= form.label t(:label_name) %>
		<br />
		<%= form.text_field :name %>
	</p>
	<p>
		<%= form.label t(:label_description) %>
		<br />
		<%= form.text_area :description %>
	</p>
</fieldset>
