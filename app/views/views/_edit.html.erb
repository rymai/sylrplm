<fieldset>
	<legend>
		<%= t(:legend_view) %>
	</legend>
	<%= f.label t(:label_name) %>
	<%= f.text_field :name %>
	<br />
	<%= f.label t(:label_description) %>
	<%= f.text_area :description %>
	<br />
	<%= f.label t(:label_relations) %>
	<%= select_inout(f, @view, @relations, :ident) %>

	<%= h_input_domain(f) %>
	<br />
</fieldset>
