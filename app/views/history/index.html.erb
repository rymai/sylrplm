<h1><%=t("history_list") %></h1>
<%= render(:partial => "history/histories", :object => @entries ) %>
<div class="align-right">
    <%= @all ? '' : link_to('all', :controller => 'history') %>
    <%= will_paginate(@entries) %>
</div>
<div id="help">
    <p>
        Listing what happened in the engine.
    </p>
</div>
