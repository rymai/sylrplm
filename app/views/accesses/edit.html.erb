<h1><%=t("access_edit")%></h1>

<% form_for(@access) do |f| %>
  <%= f.error_messages %>

 <%= render(:partial => "accesses/edit",:locals => { :f => f, :fonct=>"edit" }) %> 
  


<table class='menu_bas'>
<tr>
<td><%= f.submit t("update") %></td>
<td><%= link_to t("explorer"), @access %></td>
<td><%= link_to t("list"), accesses_path %></td>
</tr>
</table>

<% end %>